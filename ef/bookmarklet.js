javascript:(()=>{if(window.__SITE_LOCAL_TTS__)return;window.__SITE_LOCAL_TTS__={};const BASE="http://127.0.0.1:8765/tts";const PRIMARY_SELECTORS=['[data-message-author-role="assistant"]','[data-role="assistant"]','[class*="assistant"][class*="message"]','.ds-markdown'];const ARTICLE_SELECTORS=['article','[role="article"]','main [class*="markdown"]','main [class*="message"]'];const UI_ANCESTORS='nav,aside,header,footer,[role="navigation"],[role="menu"],'+'[role="dialog"],[role="toolbar"],[aria-hidden="true"]';let currentAudio=null;let currentButton=null;let injecting=false;const cleanText=el=>{const clone=el.cloneNode(true);clone.querySelectorAll(".site-local-tts-controls").forEach(x=>x.remove());return String(clone.innerText||"").replace(/\s+/g," ").trim();};const visible=el=>{if(!el||el.closest(UI_ANCESTORS))return false;const r=el.getBoundingClientRect();const s=getComputedStyle(el);return r.width>120&&r.height>24&&s.display!=="none"&&s.visibility!=="hidden";};const stop=()=>{if(currentAudio){currentAudio.pause();currentAudio.src="";currentAudio=null;}if(currentButton)currentButton.textContent=currentButton.dataset.icon;currentButton=null;};const speak=async(el,button,engine)=>{if(currentButton===button&&currentAudio){stop();return;}stop();button.textContent="⏳";currentButton=button;try{const opts={method:"POST",mode:"cors",body:cleanText(el),headers:{"Content-Type":"text/plain;charset=UTF-8"}};try{opts.targetAddressSpace="loopback";}catch{}const response=await fetch(new Request(`${BASE}?engine=${encodeURIComponent(engine)}`,opts));if(!response.ok){throw new Error(await response.text()||`TTS HTTP ${response.status}`);}const blob=await response.blob();const url=URL.createObjectURL(blob);currentAudio=new Audio(url);currentAudio.onended=()=>{URL.revokeObjectURL(url);stop();};currentAudio.onerror=()=>{URL.revokeObjectURL(url);button.textContent="❌";currentAudio=null;currentButton=null;};button.textContent="⏹";await currentAudio.play();}catch(error){console.error(`[Site Local TTS:${engine}]`,error);button.textContent="❌";button.title=String(error).trim();currentAudio=null;currentButton=null;}};const uniqueLogicalBlocks=list=>{const out=[];for(const el of list){if(!visible(el))continue;const text=cleanText(el);if(text.length<40)continue;if(out.some(x=>x===el||x.contains(el)||el.contains(x)))continue;out.push(el);}return out;};const queryOrdered=selectors=>{const found=[];for(const selector of selectors){document.querySelectorAll(selector).forEach(el=>found.push(el));}return uniqueLogicalBlocks(found);};const fallbackBlocks=()=>{const root=document.querySelector("main");if(!root)return[];const found=[...root.querySelectorAll("article,section,[role='article']")].filter(el=>{if(!visible(el))return false;const text=cleanText(el);if(text.length<120||text.length>30000)return false;const substantial=[...el.children].filter(c=>cleanText(c).length>120).length;return substantial<=4;});return uniqueLogicalBlocks(found);};const candidates=()=>{const primary=queryOrdered(PRIMARY_SELECTORS);if(primary.length)return primary;const articles=queryOrdered(ARTICLE_SELECTORS);if(articles.length)return articles;return fallbackBlocks();};const makeButton=(icon,label,engine,el)=>{const b=document.createElement("button");b.type="button";b.textContent=icon;b.dataset.icon=icon;b.title=label;b.setAttribute("aria-label",label);b.style.cssText="margin:0 3px;padding:2px 7px;border:1px solid #7776;border-radius:6px;"+"background:transparent;color:inherit;cursor:pointer;font:14px system-ui;"+"line-height:1.4;vertical-align:middle";b.addEventListener("click",event=>{event.preventDefault();event.stopPropagation();speak(el,b,engine);});return b;};const addButtons=()=>{if(injecting)return;injecting=true;try{candidates().forEach(el=>{if(el.dataset.siteLocalTtsBound==="1")return;el.querySelectorAll(".site-local-tts-controls").forEach(x=>x.remove());const controls=document.createElement("span");controls.className="site-local-tts-controls";controls.dataset.siteLocalTts="1";controls.style.cssText="display:inline-flex;align-items:center;gap:2px;margin:4px 6px 4px 0";controls.append(makeButton("🔊","Read with eSpeak NG","espeak",el),makeButton("🎙️","Read with Piper neural voice","piper",el));el.dataset.siteLocalTtsBound="1";el.prepend(controls);});}finally{injecting=false;}};document.querySelectorAll(".site-local-tts-controls").forEach(x=>x.remove());document.querySelectorAll("[data-site-local-tts-bound]").forEach(x=>delete x.dataset.siteLocalTtsBound);addButtons();const observer=new MutationObserver(mutations=>{const external=mutations.some(m=>!m.target.closest?.(".site-local-tts-controls"));if(external)addButtons();});observer.observe(document.body,{childList:true,subtree:true});window.__SITE_LOCAL_TTS__.observer=observer;})();
